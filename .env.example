# Retrocast Configuration
# Copy this file to .env and fill in your values

# Database
DB_PATH=./data/scenarios.db

# Data directories
DATA_DIR=./data
CSV_IMPORT_DIR=./data/csv

# Backend server
HOST=127.0.0.1
PORT=8000

# Norgate Data (optional – leave empty if not using Norgate)
# No API key needed – Norgate uses local installed data
